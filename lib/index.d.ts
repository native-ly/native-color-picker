import { Props } from './interfaces';
declare const NativeColorPicker: ({ animate, colors, columns, gradient, horizontal, itemSize, marker, markerStyle, onSelect, selectedColor, shadow, sort, ...props }: Props) => JSX.Element;
export default NativeColorPicker;
